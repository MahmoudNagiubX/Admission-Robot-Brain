# Core Environment & Configuration
python-dotenv==1.0.1

# HTTP Client for General APIs (Gladia, ElevenLabs, etc.)
requests==2.31.0
websockets==12.0

# LLM Integration (OpenAI / Gemini / Groq)
openai==1.30.1

# Speech-to-Text (STT) Engines
deepgram-sdk==3.3.0
speechmatics==1.6.0

# Text-to-Speech (TTS) Engine
edge-tts==6.1.12
pygame==2.5.2
azure-cognitiveservices-speech==1.37.0

# Hardware Audio Utilities (Microphone & Volume Tests)
PyAudio==0.2.14

# Terminal Display (Optional for RTL text support)
arabic-reshaper==3.0.0
python-bidi==0.6.3

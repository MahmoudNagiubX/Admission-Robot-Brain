# Environment configuration
python-dotenv==1.0.1

# LLM integration for OpenAI-compatible providers (OpenAI / Groq)
openai==1.30.1

# Local CLI text-to-speech playback and optional service-side audio generation
edge-tts==6.1.12
pygame==2.5.2

# Local CLI microphone capture
PyAudio==0.2.14

# Optional RTL terminal display support
arabic-reshaper==3.0.0
python-bidi==0.6.3

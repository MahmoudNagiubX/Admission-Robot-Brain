# Legacy Speech-to-Text Experiments

The files in this folder are legacy experiments and are not used by the current `stt_engine.py` or `tts_engine.py`.
The active STT implementation uses Deepgram via `urllib` directly in `stt_engine.py`.
The active TTS implementation uses `edge-tts` in `tts_engine.py`.

Keep these files for reference only.
